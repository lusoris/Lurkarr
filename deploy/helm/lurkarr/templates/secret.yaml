apiVersion: v1
kind: Secret
metadata:
  name: {{ include "lurkarr.fullname" . }}
  labels:
    {{- include "lurkarr.labels" . | nindent 4 }}
type: Opaque
data:
  database-url: {{ .Values.env.DATABASE_URL | b64enc | quote }}
  secret-key: {{ .Values.env.SECRET_KEY | b64enc | quote }}
