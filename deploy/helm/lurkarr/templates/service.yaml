apiVersion: v1
kind: Service
metadata:
  name: {{ include "lurkarr.fullname" . }}
  labels:
    {{- include "lurkarr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lurkarr.selectorLabels" . | nindent 4 }}
